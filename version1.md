# 일기 서비스 — Version 1 기능 명세

> 작성일: 2026-03-27
> 기술 스택: React + TypeScript, Supabase (Auth / DB / Realtime), OpenAI API, Tailwind CSS

---

## 아키텍처 개요

| 계층 | 구성 |
|------|------|
| 프론트엔드 | React 19, React Router v7, Tailwind CSS |
| 백엔드 | Supabase (PostgreSQL + Auth + Realtime) |
| AI | OpenAI API (GPT) — 없을 경우 fallback 응답 |
| 상태 관리 | React Context (AppContext) + localStorage (일부) |

---

## 1. 인증 (`/auth`)

### 작동하는 기능
- **회원가입**: 이메일 + 비밀번호 + 닉네임 입력, 이용약관·개인정보 처리방침 토글 동의 필수
- **로그인**: 이메일/비밀번호 로그인 → Supabase Auth 세션 발급 → `/app` 자동 이동
- **비밀번호 확인**: 회원가입 시 비밀번호 일치 여부 클라이언트 검증
- **비밀번호 표시/숨김**: Eye 아이콘 토글
- **한국어 에러 메시지**: Supabase 에러 코드를 한국어로 변환 (이미 가입된 이메일, 잘못된 자격증명 등)
- **인증 보호 라우팅**: 미로그인 시 `/app/*` 접근 불가 (ProtectedRoute), 로그인 시 `/` 또는 `/auth` 접근 시 `/app`으로 리다이렉트
- **세션 유지**: 페이지 새로고침 후에도 로그인 상태 유지

### 미구현 (UI만 존재)
- Google 로그인 (준비 중 표시)
- Apple 로그인 (준비 중 표시)
- 비밀번호 찾기

---

## 2. 홈 (`/app`)

### 작동하는 기능
- **오늘 날짜 표시**: 한국어 요일 포함 (예: 2026년 3월 27일 금요일)
- **알림 벨 + 드롭다운**: 읽지 않은 알림 수 배지 표시, 클릭 시 최근 5개 알림 목록 펼침
- **오늘의 일기 상태 카드**: Supabase에서 실시간으로 상태 조회 (아래 4단계)
  - `unwritten` — 오늘 아직 작성 안 함
  - `sending` — 전송 완료, 매칭 대기 중
  - `arrived` — 답장 도착
  - `completed` — 교환 완료 (답장 확인까지)
- **익명 교환 카드**: 오늘 상태 표시, "일기 작성하기" / "확인하기" 버튼
- **그룹 일기 카드**: 참여 중인 그룹 수, 오늘 작성 여부, 대기 중인 코멘트 수
- **AI 교환 카드**: 오늘 전송 여부, 마지막 AI 일기 날짜 표시
- **최근 교환 기록**: 최근 3건 목록 (날짜, 제목/내용 앞부분, 유형 배지, 완료 아이콘)
- **Realtime 상태 업데이트**: Supabase Realtime subscription으로 답장 도착 시 자동 상태 변경

---

## 3. 일기 작성 (`/app/write`) — 익명 교환 모드

### 작동하는 기능
- **하루 1회 제한**: KST 기준 오늘 날짜로 Supabase 조회, 이미 작성 시 에디터 대신 작성한 일기 표시
- **제목 + 본문 입력**: 제목 선택(optional), 본문 필수
- **종이 디자인 선택**: 줄지, 모눈, 점선, 무지, 빈티지 (기본 5종 무료)
- **스탬프 선택**: 이모지 스탬프 (감정/날씨/날짜/스티커 카테고리)
- **감정 태그 선택**: 기쁨·슬픔·화남·평온·불안·피곤·설렘·그리움 — 색상 뱃지로 표시
- **글자 수 실시간 표시**
- **전송 전 미리보기 바텀시트**: 일기 카드 미리보기 + "전송하기" / "취소" 버튼
- **콘텐츠 모더레이션**: 부적절한 표현 감지 → 전송 차단 토스트, 반복 문자 감지 → 전송 차단 토스트
- **서버사이드 중복 방지**: daily limit 체크 API 호출
- **전송 성공 후 상태 전환**: 에디터 → 작성 완료 뷰, 홈 상태 카드 `sending`으로 갱신
- **기본 종이/스탬프 기억**: Customize에서 저장한 기본값을 localStorage에서 불러옴

---

## 4. 받은 일기 (`/app/received`) — 익명 교환 수신자 뷰

### 작동하는 기능
- **Supabase에서 받은 일기 로드**: `diary_matches` 기준으로 매칭된 익명 일기 조회
- **받은 일기 원문 렌더링**: 종이 디자인, 스탬프, 감정 태그 포함
- **코멘트 작성**: 최소 30자 이상, `submitComment()` API로 Supabase에 저장
- **코멘트 전송 후 상태 전환**: 입력창 → 전송 완료 메시지
- **AI 자동 fallback**: 24시간 내 미답장 익명 일기에 AI가 자동 코멘트 생성 (페이지 마운트 시 체크)
- **신고 버튼**: UI 존재 (기능 연결 여부 미확인)

---

## 5. 그룹 교환 (`/app/group`)

### 작동하는 기능
- **내 그룹 목록**: Supabase에서 `group_members` 조회
- **공개 그룹 탐색**: 공개 그룹 목록 조회, 검색 지원
- **그룹 생성**:
  - 그룹명, 최대 인원(2~20), 공개/비공개, 설명
  - 초대 방식: 링크 또는 비밀번호
  - 생성 후 초대 코드 발급
- **그룹 참여**: 공개 그룹 바로 참여 / 초대 링크(`/join/:inviteCode`)로 참여 / 비밀번호로 참여
- **그룹 상세 뷰**:
  - 상태 탭: 오늘의 그룹 작성 현황, 멤버별 완료 여부
  - 작성 탭: 종이·스탬프·감정 데코레이션 포함 일기 작성 → Supabase 저장
  - 멤버 탭: 멤버 목록, 그룹장 강퇴/위임 드롭다운 (그룹장 전용)
- **그룹 내 일기 교환**: 그룹원 일기 수신 → 코멘트 작성 → 상태 업데이트
- **그룹 탈퇴**: 그룹 상세에서 탈퇴 기능
- **하루 1회 작성 제한**: 그룹 일기도 오늘 이미 작성 시 재작성 불가

---

## 6. AI 교환 (`/app/ai`)

### 작동하는 기능
- **페르소나 선택**: 따뜻한 친구 / 냉철한 관찰자 / 시인 (3종 기본)
- **나만의 AI 커스텀 프롬프트**: Premium 전용, 커스텀 시스템 프롬프트 저장 (Supabase profiles)
- **일기 작성 + 전송**: 종이·스탬프·감정 데코레이션 포함, Supabase에 저장
- **AI 코멘트 생성**: OpenAI API 호출 → 없을 경우 페르소나별 fallback 텍스트 반환
- **전송 후 AI 답장 탭**: 받은 AI 코멘트 목록 표시 (날짜, 페르소나 레이블, 내용)
- **AI 코멘트 다운로드**: 텍스트 파일로 저장 기능
- **하루 1회 전송**: KST 기준 오늘 이미 전송 시 재전송 불가 (상태 조회로 확인)
- **Premium 여부 확인**: `profile.subscription_type === 'premium'`으로 유료 기능 잠금/해제

---

## 7. 아카이브 (`/app/archive`)

### 작동하는 기능
- **Supabase에서 전체 일기 목록 조회**: `diaries` + `comments` 조인
- **월별 탐색**: 이전/다음 달 이동 버튼
- **유형 필터**: 전체 / 익명 / 그룹 / AI
- **일기 상세 뷰**:
  - 종이 디자인, 스탬프, 감정 태그 렌더링
  - 받은 코멘트 표시 (AI 생성 여부 표기 포함)
  - 교환 유형 배지, 작성 날짜
- **상태 배지**: 대기 중 / 완료

---

## 8. 꾸미기 (`/app/customize`)

### 작동하는 기능
- **테마 변경**: 라이트 / 다크 / 파스텔 (CSS 변수 기반, `<html>` 클래스 토글)
- **기본 종이 선택**: 줄지·모눈·점선·무지·빈티지 (무료), 파스텔·봄·여름 (Premium 잠금)
- **기본 스탬프 선택**: 감정/날씨/날짜/스티커 카테고리별 이모지
- **설정 저장**: localStorage에 `defaultPaper`, `defaultStamp` 저장 → 일기 작성 시 자동 적용
- **Premium 잠금 UI**: 비프리미엄 사용자에게 잠금 아이콘 표시

---

## 9. 마이페이지 (`/app/mypage`)

### 작동하는 기능
- **프로필 표시**: 닉네임, 이메일 (Supabase Auth에서 조회)
- **닉네임 수정**: 인라인 편집, 저장 시 Supabase `profiles` 테이블 업데이트 + localStorage 동기화
- **활동 통계**: 작성한 일기 수, 받은 답장 수, 참여 그룹 수, AI 교환 수
- **알림 목록**: 전체 알림 표시, "전체 읽음 처리" 버튼
- **로그아웃 버튼**: UI 존재 (toast 표시)

---

## 10. 알림 시스템

### 작동하는 기능
- **Supabase Realtime subscription**: `notifications` 테이블 INSERT 감지 → 실시간 UI 반영
- **DB 트리거 연동**: 익명 교환 코멘트 등록 시 발신자에게 알림 자동 생성
- **알림 유형**: `comment` (답장 도착), `group` (그룹 관련), `ai` (AI 답장)
- **읽음 처리**: `markAllRead()` — 전체 읽음 상태 변경
- **미읽음 배지**: 홈 상단 벨 아이콘에 빨간 점 표시

---

## 11. 라우팅 구조

```
/                   → 랜딩 페이지
/auth               → 로그인 / 회원가입
/app                → 홈 (ProtectedRoute)
/app/write          → 일기 작성 (익명)
/app/received       → 받은 일기
/app/group          → 그룹 교환
/app/ai             → AI 교환
/app/archive        → 아카이브
/app/customize      → 꾸미기 설정
/app/mypage         → 마이페이지
/join/:inviteCode   → 그룹 초대 링크 진입
*                   → 404 Not Found
```

---

## 12. 데이터 흐름 요약

| 데이터 | 저장소 |
|--------|--------|
| 사용자 세션/인증 | Supabase Auth |
| 일기 원문, 교환 상태 | Supabase `diaries` 테이블 |
| 매칭 정보 | Supabase `diary_matches` 테이블 |
| 코멘트 | Supabase `comments` 테이블 |
| 그룹 정보 | Supabase `groups`, `group_members` 테이블 |
| 알림 | Supabase `notifications` 테이블 |
| 사용자 프로필/닉네임 | Supabase `profiles` 테이블 |
| 테마, 기본 종이/스탬프 | localStorage |
| 그룹 일기·AI 일기 당일 작성 여부 | localStorage (날짜 키) |

---

## 13. 알려진 제한사항 / 미구현

- **로그아웃**: MyPage에 버튼 있으나 Supabase `signOut()` 미연결 (toast만 표시)
- **비밀번호 찾기**: Auth 페이지에 버튼 있으나 기능 없음
- **소셜 로그인**: Google / Apple "준비 중" 상태
- **아카이브 최근 기록 (홈)**: localStorage `archiveEntries` 사용 중 (Supabase 직접 조회 아님)
- **그룹 수 (홈/마이페이지)**: localStorage `myGroups` 사용 중
- **Premium 구독 결제**: UI 잠금만 있고 결제 플로우 없음
- **신고 기능**: 받은 일기 신고 버튼 UI만 존재
