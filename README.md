
  # Create as shown

  This is a code bundle for Create as shown. The original project is available at https://www.figma.com/design/t9RtM7jwFg8E5MMZgYN2mE/Create-as-shown.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  